# Force LF line endings for files that run inside Linux containers.
# Prevents CRLF issues when cloning on Windows (git autocrlf=true).
* text=auto
*.sh text eol=lf
*.sql text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
